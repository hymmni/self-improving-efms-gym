# Python
__pycache__/
*.py[cod]

# Harness — git은 gym 환경 코드만 추적한다 (harness 파일은 로컬 전용)
# 앞에 '/'를 붙여 레포 루트에만 적용시킨다 — 앵커 없이 'docs/'처럼 두면 mani_sim/docs/,
# mani_sim/src/mani_sim/scripts/ 같은 서브프로젝트 내부의 같은 이름 디렉토리까지 전부
# 무시돼버린다(mani_sim 통합 때 실제로 발견됨: mani_sim의 scripts/train.py가 이 규칙 때문에
# 조용히 추적 안 될 뻔했다).
.claude/
/docs/
/scripts/
/experiments/
/phases/
/references/
CLAUDE.md
continuation_plan.md

# 대용량 데이터/체크포인트 (ADR-002)
*.zarr
*.hdf5
*.pth
checkpoints/

# pointmass_notebook.ipynb 실행 시 생성되는 런타임 산출물 (재실행으로 재생성 가능)
pointmass_dataset*.pkl
ten_tight_pointmass_*.png
__temp__.mp4

# 관찰 도구 산출물 (플롯 등, 재생성 가능)
outputs/

# 데모 데이터셋 (재생성 가능, ADR-002)
data/

# phase 2/3 실험 산출물 (플롯/CSV/영상, 재생성 가능) — 단 summary류 md는 추적
results/*
!results/summary.md
!results/diag_multimodal/
results/diag_multimodal/*
!results/diag_multimodal/summary.md
!results/diag_multimodal/mu_sigma_relationship.md
!results/obstacle_env/
results/obstacle_env/*
!results/obstacle_env/summary.md
