name: diffusion_unet
obs_horizon: 2
pred_horizon: 16
action_horizon: 8
num_kp: 32
down_dims: [512, 1024, 2048]
kernel_size: 5
n_groups: 8
diffusion_step_embed_dim: 128
num_train_timesteps: 100
beta_schedule: squaredcos_cap_v2
num_inference_steps: 100
crop_hw: [76, 76]
# stage-conditioning(2026-08-04) — task.num_stages 없는 task엔 무시됨(하위호환).
# 정당화 근거는 robocasa/research_design.md "임베딩 크기를 어떻게 정할지" 절 —
# 이론으로 정해진 값 아니고 실측 스윕 잠정 1위, 재검증 필요.
stage_embed_dim: 32
